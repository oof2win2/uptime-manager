mutation createAuthCode {
  createAuthCode {
    errors {
      field
      error
		}
    code {
      createdAt
    }
  }
}

mutation removeAuthCode($code: String!) {
  removeAuthCode(code:$code) {
    errors {
      field
      error
		}
    code {
      code
      createdAt
    }
  }
}